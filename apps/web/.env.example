# Copy to .env.local. Never commit .env.local — it is a database credential.
#
# Neon, created in an EU region (ADR-0020 §4: personal data of Spanish shoppers
# does not leave the union because of where we chose to deploy). The pooled
# connection string is the right one for serverless routes.
DATABASE_URL="postgresql://user:password@ep-something-pooler.eu-central-1.aws.neon.tech/codroon?sslmode=require"
